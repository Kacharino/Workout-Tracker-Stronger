services:
  db:
    image: postgres:16
    container_name: stronger-db
    environment:
      POSTGRES_DB: stronger
      POSTGRES_USER: stronger
      POSTGRES_PASSWORD: stronger
    ports:
      - "5432:5432"
    volumes:
      - stronger_pgdata:/var/lib/postgresql/data

volumes:
  stronger_pgdata: